version: '2.2'

services:

  cli:
    build:
      context: .
      dockerfile: ./Dockerfile
    command: ["sh", "-c", "python -m system"]
    restart: always
    volumes:
    - .:/code
    stdin_open: true
    tty: true
